import * as React from 'react'
import Button from '@material-ui/core/Button';

export class RegisterView extends React.PureComponent {
	render() {
		return <Button type="primary" variant="contained" color="primary">Button</Button>;
		}
}